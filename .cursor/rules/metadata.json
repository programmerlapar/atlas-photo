{
  "description": "PHOTO_MAP_PROJECT_INDEX — Master reference for PhotoMap (Electron desktop gallery app modeled after Apple Photos). Bucket 1 implementation on 2026-07-16 added a persistent PhotoIndex, bounded cold EXIF and thumbnail work, renderer thumbnail batching, demand-loaded gallery cards, and thumbnail-first detail loading. Manual 5,000-photo timing validation remains.",
  "rules": [
    "Copy-once then delete semantics confirmed for user files (atomic rename pattern)",
    "Smart albums deferred for v2/v3 iteration",
    "Performance focus: persistent photo index, bounded thumbnail work, file watcher real-time updates, and demand-loaded gallery cards",
    "Bucket 1 is implemented; manually verify cold and warm behavior with a 5,000-photo library before moving to Bucket 2"
  ],
  "_note_for_next_session": "Read /PROJECT_ROOT/PROJECT_INDEX.md and BUCKETS_PROTOCOL.md first. Bucket 1 is implemented but still needs a real 5,000-photo cold/warm timing check."
}
