Please review all pages for adherence to our design system and tokens. There are inconsistencies in design implementation, with several components not matching our defined styles and tokens (refer to `/uiux-bot_v1`). Ensure all UI components are consistent, unified, and match the design guidelines.

If significant inconsistencies or repetitive styles are found, run `/refactor-ui-bot` to help normalize and refactor the UI for better consistency and maintainability.
